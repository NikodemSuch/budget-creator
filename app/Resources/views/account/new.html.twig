{% extends 'account/base.html.twig' %}

{% block pageContent %}

  <div class="row">
    <div class="form-container col-sm-6 col-sm-offset-3">

      <div class="form-header">
        <h2 class="form-title">Account creation</h2>
      </div>

      {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="form-button btn btn-primary btn-lg btn-block" type="submit">Create New</button>
      {{ form_end(form) }}

      <div class="form-footer">
        <p><a href="{{ path('account_index') }}">Back to the list</a></p>
      </div>

    </div>
  </div>

{% endblock %}
