{% extends 'default/base.html.twig' %}

{% block page %}

  <div class="container">
    <div class="row">
      <div class="col-sm-6 col-sm-offset-3">
        <div class="page-section">
          <h2>Category group</h2>

          <table class="table">
            <tbody>
              <tr>
                <th>Id:</th>
                <td>{{ categoryGroup.id }}</td>
              </tr>
              <tr>
                <th>Name:</th>
                <td>{{ categoryGroup.name }}</td>
              </tr>
              <tr>
                <th>Default category:</th>
                <td>{{ categoryGroup.defaultCategory.name }}</td>
              </tr>
            </tbody>
          </table>

          <div class="form-errors">
            {% for label, messages in app.flashes %}
              {% for message in messages %}
                <div class="alert alert-danger flash-{{ label }}" role="alert">
                  {{ message }}
                </div>
              {% endfor %}
            {% endfor %}
          </div>

          <a class="form-button btn btn-primary btn-lg btn-block" href="{{ path('categorygroup_edit', { 'id': categoryGroup.id }) }}">Edit</a>
          {{ form_start(delete_form) }}
            <button class="form-button btn btn-primary btn-lg btn-block" type="submit">Delete</button>
          {{ form_end(delete_form) }}
          <a href="{{ path('categorygroup_index') }}">Back to the list</a>

        </div>
      </div>
    </div>
  </div>

{% endblock %}
